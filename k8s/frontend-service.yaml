apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: hh3d-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort # Expose ra ngoài để truy cập từ trình duyệt trên Minikube
